/* This code if you want to complete the project
 to make it a web application, but currently there 
 is no need to do so, because it just a Music Player :)
 */


 /*
var http = require('http');
var fs = require('fs');


http.createServer(function (req, res) {
  res.writeHead(200, {'Content-Type': 'text/html'});
  
  fs.createReadStream('main.html').pipe(res);

}).listen(8080);
*/